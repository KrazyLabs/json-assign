{
  "name": "json-model-assign",
  "version": "1.0.3",
  "description": "Pure function for emitting factories for building models from JSON defaults, reusing the Object.assign signature and including derived non-null type validation",
  "main": "index.js",
  "repository": "git@github.com:KrazyLabs/json-model-assign.git",
  "author": "b3ngineer",
  "license": "Apache-2.0",
  "scripts": {
    "test": "mocha test"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  }
}
